name: India Sentiment Trader CI

on:
  push:
    paths:
      - "india-market-sentiment-trader/**"
  pull_request:
    paths:
      - "india-market-sentiment-trader/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: india-market-sentiment-trader
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package
        run: pip install -e ".[dev]"
      - name: Ruff
        run: ruff check src tests
      - name: Pytest
        run: pytest -q
